# User Preferences

## Tool Usage
- Use simple (single) tool calls, not compound/parallel calls. Compound calls always require authorization approval, which is unnecessary friction when the individual calls would not.
